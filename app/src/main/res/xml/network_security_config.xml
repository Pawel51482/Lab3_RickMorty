<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- TYLKO dla buildu debug: trust system + user CAs -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>
</network-security-config>
